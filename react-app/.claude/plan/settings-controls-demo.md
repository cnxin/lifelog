# 设置页控件美化 Demo 方案

**项目**: LifeLog React App  
**目标**: 先做一版设置/表单控件视觉 demo，确认效果后再批量替换原始控件  
**创建时间**: 2026-05-08

---

## 范围

本次先做 demo，不提交 GitHub，不构建 APK，不批量改业务页面。

## 执行清单

- [x] 1. 扫描当前项目中原始 select / date / number / time / checkbox 控件分布
- [x] 2. 设计一套 LifeLog 风格的统一控件视觉
- [x] 3. 新增一个临时 Demo 区块或页面用于预览效果
- [x] 4. 运行 TypeScript 检查
- [x] 5. 启动开发服务器，给出本地预览地址和查看路径
- [ ] 6. 根据确认后的样式，再规划批量替换范围

## 追加调整

- [x] A. 默认城市/默认关系/默认心情统一右侧控件宽高
- [x] B. 默认关系/默认心情改为自定义弹出菜单，避开原生 select 下拉菜单
- [x] C. 移除 Demo 里的原生 select 示例，保留 date/time/number/toggle 待继续评估
- [x] D. 修复自定义菜单被下方/上方卡片遮挡：改为页面级浮层，不再撑开布局

## Demo 设计方向

- 保留原生控件能力，先通过 wrapper + CSS 打磨视觉。
- 统一 select / date / time / number 的高度、圆角、边框、焦点态和触控尺寸。
- checkbox/toggle 使用现有提醒开关风格。
- 不引入第三方 UI 库。

## 进度追踪

- **总进度**: ✅ 5/6
