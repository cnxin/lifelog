# 变更日志

## Unreleased

- 暂无未发布变更。

## v0.1.0-test.109 (2026-06-08)

### 使用体验

- 数据管理页新增完整备份状态、上次备份文件和导入预检卡，区分覆盖恢复和分享导入，降低误操作风险。
- Notion 同步结果默认压缩为关键数字，失败原因和分类型明细改为按需展开，并补充常见网络、权限和数据库错误提示。
- 快速记录回忆进一步简化为先写一句，模板提示默认收起，人物和地点选择器新增常用推荐，减少搜索输入。
- 提醒中心的纪念日安排完成动作改为完成并记录，设置页低频控件预览折叠，减少普通用户首屏信息量。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.109.apk`。
- Android `versionCode` 更新为 `109`，`versionName` 更新为 `0.1.0-test.109`。

## v0.1.0-test.108 (2026-06-08)

### 使用体验

- Notion 同步页改为连接配置、数据同步、自动同步队列、同步历史和高级配置五个折叠区，减少首屏信息量。
- 连接配置改成 4 步就地展开，Token、父页面、自动建库和测试连接都在对应步骤内完成，不再跳到页面下方。
- 引导完成后不再显示同步作为第 5 步，手动同步统一放到数据同步工具区，避免和连接流程混在一起。
- 同步预览和数据库字段体检布局进一步压缩，修复窄屏下按钮重叠、内容裁切和卡片留白过多的问题。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.108.apk`。
- Android `versionCode` 更新为 `108`，`versionName` 更新为 `0.1.0-test.108`。

## v0.1.0-test.107 (2026-06-06)

### 使用体验

- Notion 同步新增数据库字段体检，可检查 LifeLog 需要的中文字段是否完整，缺失字段支持一键补齐。
- 保存人物、地点、回忆和纪念日安排后会进入自动同步队列，连续编辑会去重合并，避免频繁并发请求 Notion。
- 设置页新增自动同步队列，可查看待同步、同步中和失败项，并支持立即同步或单条重试。
- 人物、地点和回忆列表新增 Notion 同步状态徽标，可直接看到未同步、待同步、同步中、同步失败和已同步状态。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.107.apk`。
- Android `versionCode` 更新为 `107`，`versionName` 更新为 `0.1.0-test.107`。

## v0.1.0-test.106 (2026-06-06)

### 使用体验

- Notion 同步页新增连接体检和同步预览，可提前看到 Token、父页面、数据库、连接环境和各类数据同步数量。
- 自动创建 Notion 数据库成功后会直接测试连接，减少一次手动点击。
- 同步结果新增人物、地点、回忆和安排的分类型统计，并保留最近同步历史。
- 同步失败会记录具体失败项，历史记录中可一键重试失败内容，也支持只同步某一类数据。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.106.apk`。
- Android `versionCode` 更新为 `106`，`versionName` 更新为 `0.1.0-test.106`。

## v0.1.0-test.105 (2026-06-05)

### 使用体验

- Notion 同步页改成 5 步连接向导，按 Token、父页面、自动建库、测试连接和同步数据逐步引导。
- 顶部主操作会根据当前状态自动切换为填写 Token、填写父页面、自动创建、测试连接或同步全部。
- Notion 按钮重新分层为主按钮、外链胶囊、辅助按钮和危险按钮，移动端也会自动换行排布。
- 修改 Token、父页面或数据库 ID 后会清空旧连接状态，同步时也会使用刚保存的最新配置。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.105.apk`。
- Android `versionCode` 更新为 `105`，`versionName` 更新为 `0.1.0-test.105`。

## v0.1.0-test.104 (2026-06-05)

### 使用体验

- 设置页新增“实验”入口，Notion 同步作为试验功能单独进入，避免和常规数据管理混在一起。
- 支持保存 Internal Integration Token 和父页面 ID，并可在父页面下自动创建人物、地点、回忆、纪念日安排四类中文字段数据库。
- Notion 同步会保存本地页面映射，重复同步不会重复创建页面，Notion 页面被删除后可自动重新创建。
- Web 开发环境新增 Notion 代理和请求诊断，连接失败时可复制诊断信息，方便区分 CORS、网络和权限问题。
- 新增 Notion 连接与同步 mock 回归脚本，覆盖权限错误、网络失败、字段过滤、跳过未变更和页面重建。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.104.apk`。
- Android `versionCode` 更新为 `104`，`versionName` 更新为 `0.1.0-test.104`。

## v0.1.0-test.103 (2026-06-04)

### 使用体验

- 分享导入页新增从相册识别 LifeLog 二维码，收到二维码图片时可直接选择图片解析。
- 分享导入预览会列出将新增、将复用、将跳过和未包含的具体条目，导入前更容易判断影响范围。
- 首页合并今日行动和记录建议，改为一个“今天先做这件事”主卡，降低首屏重复感。
- 相机扫码和相册识别共用二维码识别工具，不支持识别的 WebView 会提示改用扫码或粘贴链接。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.103.apk`。
- Android `versionCode` 更新为 `103`，`versionName` 更新为 `0.1.0-test.103`。

## v0.1.0-test.102 (2026-06-04)

### 使用体验

- 分享二维码升级为 Q2 精简结构，显著降低二维码密度，同时继续兼容旧 Q1 二维码导入。
- 分享面板区分二维码、复制链接和分享包，导入二维码精简版时会明确提示内容范围。
- 分享导入成功后可直接查看新增内容，批量导入地点或回忆会临时筛出刚导入的记录。
- 首页新增独立的历年今日，回忆页新增按年份密度展示的时间地图，方便回看和跳转。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.102.apk`。
- Android `versionCode` 更新为 `102`，`versionName` 更新为 `0.1.0-test.102`。

## v0.1.0-test.101 (2026-06-04)

### 使用体验

- 分享二维码改为专用低密度编码，扫码内容默认使用精简版，减少二维码密集程度。
- 精简二维码保留标题、日期、人物和地点名称，完整正文、地址、链接和照片继续使用复制链接或分享包。
- Android 新增 `lifelog://q` 深链识别，外部扫码也能唤起 LifeLog 导入精简分享。
- 保存二维码到相册成功后，二维码弹窗内会持续显示保存成功提示，避免错过 Toast。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.101.apk`。
- Android `versionCode` 更新为 `101`，`versionName` 更新为 `0.1.0-test.101`。

## v0.1.0-test.100 (2026-06-04)

### 使用体验

- 分享面板中的二维码支持点击放大，单独弹窗展示更适合扫码和确认。
- 二维码弹窗新增保存到相册和分享图片，Android 可调用系统相册与系统分享面板转发到微信等应用。
- LifeLog 分享链接改为更紧凑的二维码编码格式，同时保留旧链接解析兼容。
- 二维码生成降低纠错密度，内容较少时二维码更清爽；大内容仍建议使用分享包。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.100.apk`。
- Android `versionCode` 更新为 `100`，`versionName` 更新为 `0.1.0-test.100`。

## v0.1.0-test.99 (2026-06-03)

### 使用体验

- 回忆详情合并重复的故事卡、内容卡和标签卡，首屏改为单一正文阅读卡。
- 回忆详情恢复折叠头部，并为正文卡预留安全缓冲，减少折叠时遮挡正文和心情标签。
- 关联人物和关联地点搜索支持多关键词与去空格匹配，地点搜索会按店名、商场、城市和地址排序。
- 检查更新增加检查中来源提示，慢源会标记为已跳过，减少用户误以为卡住。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.99.apk`。
- Android `versionCode` 更新为 `99`，`versionName` 更新为 `0.1.0-test.99`。

## v0.1.0-test.98 (2026-06-03)

### 使用体验

- 检查更新改为优先读取 Gitee、CDN 和 GitHub Release，拿到可用结果后不再等待较慢的 GitHub raw 清单。
- GitHub raw 清单保留为兜底来源，主来源不可用时才会启用，减少弱网下长时间等待。
- 首页、日历、提醒中心、数据体检和回忆详情的纪念日入口会带上具体纪念日，进入人物详情后直接定位到对应卡片。
- 人物详情纪念日跳转增加顶部遮挡补偿，并移除会造成回滚的多次自动校正。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.98.apk`。
- Android `versionCode` 更新为 `98`，`versionName` 更新为 `0.1.0-test.98`。

## v0.1.0-test.97 (2026-06-03)

### 使用体验

- 首页新增本地记录建议，会根据纪念日、已完成安排、今日记录、收藏人物和新地点提示下一步可以记录什么。
- 记录建议点击后可直接进入对应安排、人物详情或快速回忆，并自动带入人物、地点和建议文案。
- 日历选中日期新增概览卡，集中显示相对今天、农历周次、当天回忆、重要日子、人物和地点数量。
- 日历支持通过 `date` 参数直接跳到指定月份，空日期只保留概览和补记入口，减少重复空状态。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.97.apk`。
- Android `versionCode` 更新为 `97`，`versionName` 更新为 `0.1.0-test.97`。

## v0.1.0-test.96 (2026-06-02)

### 使用体验

- 日历选中任意日期后会显示今天、几天前或几天后，空状态也会提示相对今天的距离。
- 人物详情的纪念日安排新增管理入口，可统一查看年度安排和节点安排。
- 纪念日安排管理支持继续添加、编辑、删除安排，并保留打开关联回忆的操作。
- 纪念日卡片外层入口收敛为安排和历史，历史页同时展示往年安排与节点历史，减少重复按钮。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.96.apk`。
- Android `versionCode` 更新为 `96`，`versionName` 更新为 `0.1.0-test.96`。

## v0.1.0-test.95 (2026-06-02)

### 使用体验

- 回忆、人物和地点列表默认只保留搜索、结果数和一个筛选或排序入口，减少首屏按钮密度。
- 回忆筛选的人物、地点、心情和标签改为按需展开，已选条件仍会以标签提示当前列表状态。
- 人物页排序默认收起，只有需要切换智能、最近或名称排序时再展开。
- 地点页国家、省市、区域、分类和排序统一收入筛选面板，保留清除筛选和条件数量提示。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.95.apk`。
- Android `versionCode` 更新为 `95`，`versionName` 更新为 `0.1.0-test.95`。

## v0.1.0-test.94 (2026-06-02)

### 使用体验

- 回忆详情首屏新增故事回看卡，优先展示这件事、日期心情、人物地点、照片和标签摘要。
- 故事卡提供补充细节、查看关联人物或地点、再记一件相关回忆和分享入口，减少字段式详情带来的割裂感。
- 从回忆详情再记一件时会自动带入当前关联人物和地点，方便连续记录同一次关系或场景。
- 原有内容、标签、照片、人物、地点和相关回忆区保留在下方，兼顾快速回看和完整整理。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.94.apk`。
- Android `versionCode` 更新为 `94`，`versionName` 更新为 `0.1.0-test.94`。

## v0.1.0-test.93 (2026-06-02)

### 使用体验

- 快速回忆新增吃饭、约会、逛街、聊天、出行、礼物等场景模板，可一键预填标题、心情、标签和补充提纲。
- 人物详情“行动中心”改为“相处小抄”，支持直接记录互动、跳转纪念日、打开常去地点和编辑喜好/雷区。
- 地点详情新增“下次怎么用”，根据地点类型、常关联人物、评分、标签和地图信息给出下一次使用建议。
- 人物和地点详情的建议卡片都接入实际操作，减少看得到但不能直接行动的入口。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.93.apk`。
- Android `versionCode` 更新为 `93`，`versionName` 更新为 `0.1.0-test.93`。

## v0.1.0-test.92 (2026-06-02)

### 使用体验

- 首页首屏新增生活场景入口，可直接选择“记一件事”“记一个人”“记一个地方”或“准备一个日子”。
- 快速回忆表单改为更生活化的表达，从“先留下这一刻”“这件事”“当时感觉”开始，降低分类和字段压力。
- 全局新增面板和详情页快捷操作改为当前页面语境，例如“记和 TA 的事”“记这里发生的事”。
- 首页快速记录收件箱和待补全提示改为先记后补的表达，减少数据管理感。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.92.apk`。
- Android `versionCode` 更新为 `92`，`versionName` 更新为 `0.1.0-test.92`。

## v0.1.0-test.91 (2026-05-31)

### 分享与导入

- 回忆详情和地点详情新增更明显的分享操作卡，分享入口不再藏在头部胶囊按钮里。
- 首页快捷新增加入“扫描二维码”，可直接进入 LifeLog 分享二维码导入。
- 分享导入页新增扫码入口，支持用相机扫描 LifeLog 分享二维码，不支持扫码的环境会提示改用粘贴链接。

### 回忆

- 移除回忆详情和保存回忆提示里的“再记一条相关回忆”入口，相关回忆区只负责回看已有记录。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.91.apk`。
- Android `versionCode` 更新为 `91`，`versionName` 更新为 `0.1.0-test.91`。

## v0.1.0-test.90 (2026-05-29)

### 纪念日

- 人物详情的纪念日卡片新增“节点历史”入口，可查看已创建过的天数节点安排、状态、进度和关联回忆。
- 回忆详情会展示反向关联的纪念日安排，包含年度安排和天数节点安排，方便从回忆追溯来源。

### 回忆

- 快速记录回忆新增常用场景和当前上下文模板，没有预选人物或地点时也能一键填入标题。
- 回忆详情照片默认展示前 9 张，照片较多时可手动展开全部，减少详情页首屏过长。

### 备份与发布

- 备份导入失败后会保留失败前预检摘要、照片检查结果和建议处理步骤，降低恢复排查成本。
- 备份导入新增安全导入模式，可在严格导入失败后跳过异常照片并保留可恢复的人物、地点和回忆。
- 新增 `release:prepare` 发布准备脚本，串联版本同步、核心回归、APK 构建、发布文件刷新和发版检查。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.90.apk`。
- Android `versionCode` 更新为 `90`，`versionName` 更新为 `0.1.0-test.90`。

## v0.1.0-test.89 (2026-05-29)

### 纪念日

- 纪念日新增天数节点配置，支持情侣、宝宝、目标和自定义模板，可生成 100 天、365 天、1000 天等节点。
- 天数节点接入首页纪念日卡片、人物详情、日历和提醒预览。
- 天数节点安排可独立记录待办、预算、关联地点和回忆，不会覆盖年度纪念日安排。
- 备份导入和数据规范化会保留天数节点配置及节点安排目标。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.89.apk`。
- Android `versionCode` 更新为 `89`，`versionName` 更新为 `0.1.0-test.89`。

## v0.1.0-test.88 (2026-05-28)

### 分享与导入

- 本地分享新增可扫码二维码，二维码在本机生成，不经过第三方二维码服务。
- Android 支持 `lifelog://share/import#...` 分享导入深链，真机扫码可直达导入页。
- 分享记录中心保留分享包、分享链接和导入记录，链接可再次复制，分享导入支持撤销。

### 提醒与安排

- 提醒中心新增今日和逾期纪念日安排处理，可直接完成或跳过。
- 完成安排后会引导继续记录回忆，减少从提醒到回忆归档的断点。

### 地点与批量管理

- 地点批量管理支持修改前预览，可统一补充分类、商场、区域和标签。
- 批量修改完成后可从提示中撤销，降低误改成本。

### 交互体验

- 首页快速记录会保留未提交草稿，今日行动空状态降噪。
- 完整备份增加最近一次导出时间提醒，长列表新增回到顶部按钮。
- 内置升级补充下载来源、速度和剩余时间展示，主源失败时会自动重试备用源。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.88.apk`。
- Android `versionCode` 更新为 `88`，`versionName` 更新为 `0.1.0-test.88`。

## v0.1.0-test.87 (2026-05-28)

### 产品分层

- 本地功能全部免费，仅为未来云同步、云备份、多设备同步和云端分享恢复保留云端高级权益。

### 分享与备份

- 分享包导入支持撤销本次新增内容，人物、地点、回忆和照片都可以回滚。
- 完整备份增加最近一次导出时间提醒，方便判断是否需要重新备份。

### 升级体验

- 内置升级补充下载来源、速度和剩余时间展示，主源失败时会自动重试备用源。

### 交互体验

- 长列表新增回到顶部按钮，数据管理和设置页滚动更直接。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.87.apk`。
- Android `versionCode` 更新为 `87`，`versionName` 更新为 `0.1.0-test.87`。

## v0.1.0-test.86 (2026-05-28)

### 导航与设置

- 底部导航收敛为首页、档案、回忆三项，人物 / 地点与时间线 / 日历改为页面内分段切换。
- 设置页重组为账号、应用、数据、关于四个平级入口，移除重复的管理中心和当前更新信息。

### 人物与录入体验

- 人物喜好档案和禁忌雷区拆分为独立标准编辑面板，列表和详情页都可直接修改。
- 关联人物 / 地点支持新增并关联，筛选状态、表单草稿和纪念日安排处理状态会自动保留。
- 分享导入加入首页快捷新增入口，回忆和地点可生成本地分享链接并导入到 LifeLog。

### 显示与平台适配

- 修复 Web 顶部黑色挖孔装饰常驻显示，并优化 Android 状态栏背景。
- 修正美团分享链接被识别为大众点评的问题。
- 账号页精简为设置入口，安装权限、提醒权限和应用能力展示更清晰。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.86.apk`。
- Android `versionCode` 更新为 `86`，`versionName` 更新为 `0.1.0-test.86`。

## v0.1.0-test.85 (2026-05-28)

### 数据体检

- 资料体检分组支持展开具体问题，缺失关联、资料补全和记录问题可直接跳转处理。

### 首页与纪念日

- 首页今日行动新增稍后和今天忽略，临时不处理的提醒不会反复占用首页。
- 纪念日安排新增生日、送礼、聚餐和出行模板，可一键填入预算、提醒、备注和待办。

### 更新体验

- 检查更新会展示 Gitee、GitHub raw、CDN 和 GitHub Release 的来源诊断，便于定位网络或缓存问题。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.85.apk`。
- Android `versionCode` 更新为 `85`，`versionName` 更新为 `0.1.0-test.85`。

## v0.1.0-test.84 (2026-05-27)

### 数据健康

- 数据管理新增分组资料体检，区分关联完整性、人物资料、地点质量、回忆与安排可补全项。
- 强重复地点可从数据管理中一键合并，并继续保留地点页的合并历史和撤销能力。
- 备份导入预检新增当前数据对比差值，孤立照片不再导致可恢复备份被完整性校验误拦截。

### 纪念日与首页

- 纪念日安排可一键复用往年安排，待办会重置为未完成，方便每年继续准备。
- 首页今日行动进一步聚焦今天需要处理的事项，减少和未来 30 天纪念日区块重复。

### 更新体验

- 关于页新增 APK 安装权限和通知权限状态，升级前可直接刷新或跳转授权。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.84.apk`。
- Android `versionCode` 更新为 `84`，`versionName` 更新为 `0.1.0-test.84`。

## v0.1.0-test.83 (2026-05-27)

### 内置升级

- 内置升级新增下载进度条，下载中会禁用重复点击，避免重复拉起升级任务。
- 发布清单新增 APK SHA256，Android 下载完成后会校验文件完整性再打开安装器。
- 安装来源权限设置返回后会自动继续本次升级，减少重复操作。
- 升级失败、校验失败或兜底下载会在更新卡片中展示当前阶段。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.83.apk`。
- Android `versionCode` 更新为 `83`，`versionName` 更新为 `0.1.0-test.83`。

## v0.1.0-test.82 (2026-05-27)

### 升级体验

- 更新页主按钮改为内置升级，优先从 Gitee 下载 APK 并自动打开系统安装器。
- 新增安装来源权限预检，未授权时会先跳转到系统设置，减少下载后安装失败。
- 外部下载保留为独立入口，并优先打开 GitHub Release APK，避免浏览器把 Gitee raw 保存成 zip。
- 更新信息同时展示内置下载源和外部下载源，升级路径更清晰。

### 验证

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.82.apk`。
- Android `versionCode` 更新为 `82`，`versionName` 更新为 `0.1.0-test.82`。

## v0.1.0-test.81 (2026-05-27)

### Gitee 下载

- 恢复 Gitee 国内镜像作为 APK 主下载源，GitHub Release 保留为兜底入口。
- Android 端改为应用内下载 APK 后用系统安装器打开，避免 Gitee raw 的 `text/plain` 响应被保存成 zip。
- 安装前会校验下载文件不为空且具备 APK 压缩包文件头，异常时自动回退到 GitHub Release。

### 发布流程

- 发布脚本重新把镜像 APK 地址写回 Gitee raw，保留国内下载链路。
- 更新检查的下载源展示重新显示 Gitee 国内镜像优先。

### 验证

- 通过 `npm.cmd run test:update-checker`、`npm.cmd run test:external-links`、`npm.cmd run build` 和 Android `:app:compileDebugJavaWithJavac`。
- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.81.apk`。
- Android `versionCode` 更新为 `81`，`versionName` 更新为 `0.1.0-test.81`。

## v0.1.0-test.80 (2026-05-27)

### 更新显示

- 修复当前版本为 `0.1.0-test.79` 时，当前更新仍显示 `0.1.0-test.78` 的打包顺序问题。
- 确保 release notes 在构建 APK 前写入，当前版本和当前更新保持一致。

### APK 下载

- Gitee 保留为更新清单来源，不再作为 APK 安装包优先下载源。
- APK 下载改回优先使用 GitHub Release 附件，避免 Android 把 Gitee raw 文件保存成 zip。
- 发布脚本默认把镜像 APK 地址写回 jsDelivr 文件镜像，安装入口继续以 GitHub Release 为准。

### 验证

- 通过 `npm.cmd run test:release-ready`、`npm.cmd run test:update-checker` 和 `git diff --check`。
- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.80.apk`。
- Android `versionCode` 更新为 `80`，`versionName` 更新为 `0.1.0-test.80`。

## v0.1.0-test.79 (2026-05-27)

### 备份体验

- 导入预检新增照片归属修复、缺失引用、孤立照片和补回关联提示，导入前能更清楚判断备份质量。
- 导出失败会在控制台记录详细错误，便于继续排查 Android 首次导出失败的原因。

### 更新检查

- 更新检查页新增下载源展示，可区分 Gitee 国内镜像、GitHub Release 和 CDN。
- 更新检查优先读取 Gitee 镜像清单，同版本时优先使用包含国内镜像的清单。

### 发布流程

- 新增 `release:prepare-files` 脚本，自动复制 APK 并回填 `update-manifest.json` 基础字段。
- README 补充 Gitee 镜像、发布脚本和备份排查说明。

### 验证

- 通过 `npm.cmd run test:backup-health`、`npm.cmd run test:backup-import`、`npm.cmd run test:update-checker`、`npm.cmd run build` 和 `git diff --check`。
- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.79.apk`。
- Android `versionCode` 更新为 `79`，`versionName` 更新为 `0.1.0-test.79`。

## v0.1.0-test.78 (2026-05-26)

### 备份导入

- 修复备份导入时照片记录 `memoryId` 指向旧回忆，导致完整性校验把有效照片丢弃的问题。
- 导入会根据回忆中的照片引用自动修复照片归属，保留有效照片。
- 导出完整备份时会同步修正照片和回忆的双向关联，避免再次生成不一致备份。

### 下载镜像

- APK 下载优先使用 Gitee 国内镜像，GitHub Release 保留为兜底。

### 验证

- 通过 `npm.cmd run test:backup-import`、`npm.cmd run test:helpers` 和 `npm.cmd run build`。
- 使用用户提供的真实备份文件验证导入归一化通过。
- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.78.apk`。
- Android `versionCode` 更新为 `78`，`versionName` 更新为 `0.1.0-test.78`。

## v0.1.0-test.77 (2026-05-26)

### Android 备份导出

- 完整备份导出在 Android 端改为调用系统文件保存器，可自行选择保存目录和文件名。
- 导出完成后会显示文件名、保存位置说明和 Android 返回的文件位置标识。
- 在系统文件保存器中取消保存时，会提示“已取消导出备份”。
- Web 端继续使用浏览器默认下载逻辑，桌面测试仍可直接下载 JSON 备份文件。

### 表单稳定性

- 表单内人物 / 地点搜索框进一步隔离内部输入框样式，避免被外层表单输入框规则覆盖。

### 验证

- 通过 `npm.cmd run build`、Android `:app:compileDebugJavaWithJavac`、`test:helpers` 和 `git diff --check`。
- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.77.apk`。
- Android `versionCode` 更新为 `77`，`versionName` 更新为 `0.1.0-test.77`。

## v0.1.0-test.76 (2026-05-26)

### 全局体验

- 底部导航升级为悬浮 Dock 样式，增加当前页小圆点、主题阴影和更清晰的按压反馈。
- 全局主题阴影改为跟随主题色，并补充暗色模式阴影变量。
- 人物详情关系摘要新增关系温度波浪背景，关系状态更直观。

### 账号与备份

- 账号管理整合“当前能力”和“应用能力”，资料管理、生活记录、提醒备份和使用方式改为独立分隔块。
- 完整备份导出后会显示文件名和保存位置说明；Android 端提示到系统下载目录或下载通知中查找。

### 表单细节

- 地点图片上传入口缩小字号、按钮高度和图标尺寸，和编辑地点表单密度更一致。
- 纪念日安排新建时不再默认生成待办事项，避免不需要的预置内容。
- 纪念日安排的关联地点搜索框改为图标列和输入列布局，修复放大镜与搜索文字重叠。

### 验证

- 通过 `npm.cmd run build`、`test:helpers` 和 `git diff --check`。
- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.76.apk`。
- Android `versionCode` 更新为 `76`，`versionName` 更新为 `0.1.0-test.76`。

## v0.1.0-test.75 (2026-05-26)

### 表单体验

- 编辑人物的生日字段改为外侧标题加日期按钮，和姓名、关系等字段保持一致。
- 纪念日编辑保留名称 / 日期小标题，并统一名称输入框、日期按钮和删除按钮的高度与基线。
- 喜好档案和禁忌 / 雷区改为纵向卡片结构，分类、标签、新增项目和删除按钮更清晰。
- 人物、地点、回忆的新增 / 编辑弹层统一普通输入、下拉、日期和数字步进控件高度；备注和正文保留原有输入空间。

### 详情与首页

- 人物详情页快捷新增面板保留“记录回忆”，并新增“编辑资料”入口，移除不符合人物详情语义的操作。
- 首页今日行动和纪念日安排减少重复提示，今日行动更聚焦需要马上处理的事项。

### 更新检查

- 更新中心会展示检查来源，便于区分 CDN 清单、GitHub raw 清单和 GitHub Release。
- 本地更新清单同步到当前版本，继续通过多来源比较减少 CDN 缓存误判。

### 验证

- 通过 `npm.cmd run build` 和 `test:helpers`。
- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.75.apk`。
- Android `versionCode` 更新为 `75`，`versionName` 更新为 `0.1.0-test.75`。

## v0.1.0-test.74 (2026-05-26)

### 纪念日安排

- 纪念日新增年度安排，可记录待办、状态、预算 / 礼物线索、关联地点、提醒节点和备注。
- 人物详情的纪念日卡片会显示当前年度安排摘要，并提供“查看安排 / 添加安排”入口。
- 记录纪念日回忆后，会自动把对应安排标记为已完成并关联回忆。

### 历史与提醒

- 同一纪念日的往年安排按年份保留，不会覆盖当前年度安排。
- 人物详情纪念日卡片新增“往年安排”按钮，打开后可查看历史安排、待办进度和关联回忆。
- 首页今日行动会提示即将到来的纪念日安排，已有安排时可继续准备。

### 界面整理

- 全局搜索空状态移除重复的快速跳转入口，避免和底部导航重复。
- 账号管理底部能力标签改为卡片展示，并减少技术栈重复信息。

### 验证

- 通过 `npm.cmd run build`、`test:helpers`、`test:reminders` 和 `test:calendar-items`。
- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.74.apk`。
- Android `versionCode` 更新为 `74`，`versionName` 更新为 `0.1.0-test.74`。

## v0.1.0-test.73 (2026-05-26)

### 检索与回看

- 顶部新增全局搜索面板，支持跨人物、地点、回忆、标签、地址、喜好和平台链接快速查找。
- 回忆详情新增“相关回忆”，会根据相同人物、地点、标签、相近日期和心情给出关联回看入口。

### 资料完整度

- 设置页新增“资料体检”，集中提示缺失生日、喜好、地点入口、照片、回忆人物 / 地点 / 标签等可补全项。
- 体检项支持直接跳转到对应人物、地点或回忆详情，减少逐个页面排查。

### 地点与提醒

- 商场列表和商场详情新增店铺到访总数、最近到访和常同行人物。
- 商场详情的“里面的地点”会展示每家店铺的到访次数、最近到访和常同行人物。
- 修复提前提醒预览把提醒触发日当成目标日的问题；纪念日还有 6 天且提前 3 天提醒时，会显示“还有 6 天 · 提前 3 天提醒”。
- 周年标签按展示场景区分：人物页显示截至今天的已满年数，提醒和未来 30 天显示目标纪念日对应的周年。

### 验证

- 通过 `npm.cmd run build`、`test:reminders`、`test:helpers`、`test:display`、`test:location` 和 `test:calendar-items`。
- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.73.apk`。
- Android `versionCode` 更新为 `73`，`versionName` 更新为 `0.1.0-test.73`。

## v0.1.0-test.72 (2026-05-25)

### 可撤销操作

- 人物、地点和回忆删除后会显示撤销入口，降低误删成本。
- 恢复人物和地点时会同步恢复受影响回忆里的关联关系。
- 恢复回忆时会保留原回忆和照片引用。

### 列表体验

- 回忆列表改为按月份时间线分组，跨年份时提供年份跳转。
- 回忆、人物和地点列表新增筛选 / 搜索命中数量，筛选后能清楚看到当前结果范围。
- 回忆和地点筛选会显示当前条件标签，便于确认为什么列表变少。
- 人物列表新增智能、最近和名称排序；地点列表新增智能、最近、评分和名称排序。
- 地点列表和详情新增到访次数、最近到访和常关联人物。

### 首页体验

- 首页新增“常去地点”，按收藏、最近到访和到访次数优先展示。
- 常去地点支持直接进入详情，也可以一键带入地点快速再记一条回忆。

### 验证

- 通过 `npm.cmd run build`、`test:helpers`、`test:display`、`test:location` 和 `test:calendar-items`。
- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.72.apk`。
- Android `versionCode` 更新为 `72`，`versionName` 更新为 `0.1.0-test.72`。

## v0.1.0-test.71 (2026-05-25)

### 快捷操作

- 全局 FAB 改为按当前页面生成快捷操作，不再固定展示同一组全局入口。
- 人物详情优先记录和当前人物相关的回忆，地点详情优先记录当前地点发生的事，并自动带入上下文。
- 首页保留完整快捷入口，人物、地点、回忆和日历页面优先展示当前页面主任务。

### 日常体验

- 首页新增“今日行动”，整合未来提醒、长期未互动人物和往年今日回忆。
- 日历页支持按选中日期补记，空日期和 FAB 都会自动带入当前选中日期。
- 保存回忆后提供“查看 / 补照片 / 再记”快捷动作，减少保存后的跳转成本。
- 照片上传错误改为 toast 和组件内摘要，不再用连续弹窗打断批量上传。
- 人物列表和人物详情新增关系温度，显示最近互动状态、回忆数量和需要联系的人。

### 验证

- 通过 `npm.cmd run build`、`test:calendar-items`、`test:quick-memory` 和 `test:display`。
- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.71.apk`。
- Android `versionCode` 更新为 `71`，`versionName` 更新为 `0.1.0-test.71`。

## v0.1.0-test.70 (2026-05-24)

### 平台链接

- 地点平台链接会优先把高德、美团、点评、抖音和小红书网页入口转换为对应 App scheme。
- 高德搜索和坐标链接优先使用 `amapuri` 打开，减少先进入 Chrome 再跳转的情况。
- 美团、点评、抖音和小红书的自动搜索入口优先打开对应 App 搜索页。
- 如果目标 App 未安装或 scheme 不可用，会自动回退到原网页链接，避免点击后无响应。

### 验证

- 扩展外链回归测试，覆盖高德、美团、点评、抖音和小红书 App scheme 转换。
- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.70.apk`。
- Android `versionCode` 更新为 `70`，`versionName` 更新为 `0.1.0-test.70`。

## v0.1.0-test.69 (2026-05-23)

### 地点显示

- 地点页“具体店铺 / 场所”现在显示所有非商场记录，商场 / 园区里的店铺也会出现在列表中。
- 保留商场 / 园区分组展示，同时给具体店铺列表提供更完整的选择范围。

### 回忆录入与展示

- 回忆关联人物和关联地点改为点击输入框后再展开候选项，支持输入内容搜索筛选。
- 候选项不再默认铺在表单外层，减少和提示文字、正文模板区域的视觉冲突。
- 回忆心情和标签拆开展示，心情使用独立样式，标签继续以标签 chip 展示。

### 外部链接

- Android 普通外链改为原生 `ACTION_VIEW` 打开，不再使用 Chrome Custom Tab。
- 地点平台入口会优先定向到高德、美团、大众点评、抖音、小红书、百度地图、腾讯地图或微信。
- 商场导航也统一使用原生地图 / 外链打开逻辑。

### 发布

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.69.apk`。
- Android `versionCode` 更新为 `69`，`versionName` 更新为 `0.1.0-test.69`。

## v0.1.0-test.68 (2026-05-23)

### 地点层级

- 修复地点编辑中清空商场 / 园区后，重启应用又被地址或区域推断回填的问题。
- 地点加载归一化现在会区分“旧数据缺少 mall 字段”和“用户主动清空 mall 字段”。

### 回忆多地点

- 回忆正文预制模板按多个关联地点分开生成，不再把多个地点合并成一条模板。
- 回忆详情中的关联地点改为标题一行、地址第二行的展示结构。
- 多地点展示新增压缩规则：超过两个地点时显示为“前两个地点 等 N 个地点”。

### 录入体验

- 地点选择器支持按地点名、分店、商场、区域、城市和地址搜索。
- 地点候选项改为两行展示，第一行显示地点 / 分店，第二行显示商场、区域、城市和地址。
- 首页、回忆列表、人物详情、地点详情和商场详情统一使用回忆卡片组件。

### 发布

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.68.apk`。
- Android `versionCode` 更新为 `68`，`versionName` 更新为 `0.1.0-test.68`。

---

## v0.1.0-test.67 (2026-05-22)

### 回忆地点关联

- 回忆关联地点从单选升级为多选，适配一次商场行程里关联多家店铺。
- 旧数据继续保留 `placeId` 主地点兼容字段，并新增 `placeIds` 多地点索引和迁移逻辑。
- 回忆列表、回忆详情、人物详情、地点详情、商场详情和备份健康检查都已适配多地点读取。
- 从多地点回忆继续记录相关回忆时，会继承全部关联地点。

### 地点资料

- 地点类型新增“杂货店”和“服装店”。
- 地点支持本地图片上传，图片压缩后保存到地点资料，也继续支持每行一个图片 URL。
- 地点照片空状态和补全提示已更新为“本地上传或图片链接”。
- 地点评分改为“未获取”状态，不再给高德、美团或点评分享默认填入 4 分。

### 分享解析

- 平台识别改为 URL 优先，修复美团链接因为文本里出现“点评”而被识别成大众点评的问题。
- 平台链接解析在标签与 URL 冲突时以 URL 为准，并自动修正平台标签。
- 分享解析样本库新增美团链接混入点评噪声、平台标签与 URL 冲突等回归样本。
- 茶饮、杂货店、便利店、超市、服装、鞋包等分类识别规则增强。

### 发布

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.67.apk`。
- Android `versionCode` 更新为 `67`，`versionName` 更新为 `0.1.0-test.67`。

---

## v0.1.0-test.66 (2026-05-22)

### 更新检查

- 版本检查改为同时读取 CDN 清单、GitHub raw 清单和 GitHub latest Release，并选择最高版本。
- 修复 jsDelivr `@main/update-manifest.json` 缓存停留在旧版本时，App 会把旧版本误判为最新版本的问题。
- GitHub Release fallback 生成的 CDN APK 镜像路径改为 `downloads/` 下的最新 APK 文件。

### 仓库整理

- 仓库根目录移除历史 APK，历史安装包统一保留在 GitHub Release。
- 移除已提交的 Chrome Web 测试临时目录，避免 main 分支包含大量缓存文件。
- 新增 `downloads/` 目录保留当前最新 APK 镜像，README 补充下载、更新和仓库结构说明。
- 账号关于页的 GitHub 项目行支持点击跳转到项目主页。

### 发布

- 生成签名 Android release APK：`downloads/lifelog-v0.1.0-test.66.apk`。
- Android `versionCode` 更新为 `66`，`versionName` 更新为 `0.1.0-test.66`。

---

## v0.1.0-test.65 (2026-05-22)

### 更新下载修复

- APK 下载改为 Android 原生 `ACTION_VIEW` 打开外部浏览器，修复点击“CDN 下载 APK”没有反应的问题。
- Web 端仍保留普通外链打开逻辑，Android 原生调用失败时回退到系统 intent URL。

### 更新区 UI

- 版本更新区改为“CDN 下载 APK”单独一行。
- “复制链接”和“查看 Release”改为等宽并列一行，统一高度、图标尺寸、文字行高和水平对齐。
- 移除挤压布局中的“GitHub 备用”按钮，备用下载能力保留在 Release 页面。

### 发布

- 生成签名 Android release APK：`lifelog-v0.1.0-test.65.apk`。
- Android `versionCode` 更新为 `65`，`versionName` 更新为 `0.1.0-test.65`。

---

## v0.1.0-test.64 (2026-05-22)

### 界面修复

- 修复首页“未来 30 天”纪念日横向卡片右侧露出异常边框 / 下一张卡片边缘的问题。
- 统一版本更新区“CDN 下载 APK”“复制链接”“GitHub 备用”“查看 Release”按钮的高度、图标尺寸、文字行高和水平对齐。

### 日历与更新

- 修复日历中点击下个月日期时不显示该日期纪念日的问题，日历事件现在按完整 42 格日历范围加载。
- 更新检查优先读取 jsDelivr 更新清单，国内网络下优先使用 CDN 下载 APK，GitHub Release 作为备用入口。
- 新增日历跨月纪念日回归测试，覆盖下个月日期在当前日历页直接显示记录。

### 发布

- 生成签名 Android release APK：`lifelog-v0.1.0-test.64.apk`。
- Android `versionCode` 更新为 `64`，`versionName` 更新为 `0.1.0-test.64`。

---

## v0.1.0-test.63 (2026-05-22)

### APK 下载热修

- 修复应用内打开 Chrome 更新时 APK 下载可能一直卡住的问题。
- “下载新版本 APK”改为调用 Android 外部浏览器 Intent，避免继续停留在应用内 Chrome Custom Tab。
- 新增“复制链接”按钮，可把 APK 下载链接复制到 Chrome 或系统浏览器手动打开。
- 保留 Release 页面查看入口，方便用户切换下载方式。

### 发布

- 新增外部链接回归测试，覆盖 APK 下载 Intent URL 生成。
- 生成签名 Android release APK：`lifelog-v0.1.0-test.63.apk`。
- Android `versionCode` 更新为 `63`，`versionName` 更新为 `0.1.0-test.63`。

---

## v0.1.0-test.62 (2026-05-22)

### 更新中心

- 账号页版本更新卡新增 APK 名称、大小、发布时间、检查时间和 GitHub Release 摘要。
- 发现新版本时同时提供 APK 下载和 Release 查看入口。
- 修复检查按钮中刷新图标和“检查”文字没有水平对齐的问题。

### 反馈诊断

- 账号页新增反馈诊断入口，可以复制版本、设备、屏幕、数据数量和备份健康状态。
- 诊断信息不包含具体人物姓名、地点名称、回忆标题或正文，便于测试反馈。
- 新增诊断信息回归测试，防止后续误把隐私内容加入诊断摘要。

### 提醒与编辑体验

- 提醒设置页新增未来 7 天提醒预览，可看到生日、纪念日、联系和回忆提醒。
- 回忆编辑新增正文结构化模板，帮助快速补充“发生了什么 / 下次可以 / 当时感受”等内容。
- 照片预览改用对象 URL 并在卸载时释放，降低多图页面反复进入的内存风险。

### 工程化

- 新增 `test:release-ready` 发版检查，覆盖版本号、README、CHANGELOG、releaseNotes、APK 和 SHA256。
- 新增提醒预览、诊断信息和更新中心元数据回归测试。
- 生成签名 Android release APK：`lifelog-v0.1.0-test.62.apk`。
- Android `versionCode` 更新为 `62`，`versionName` 更新为 `0.1.0-test.62`。

---

## v0.1.0-test.61 (2026-05-21)

### 更新内容显示

- 账号页关于区域新增当前版本更新内容展示，可以直接看到本次版本改了什么。
- 新增最近版本摘要，方便回看近几个测试版本的主要变化。
- 更新说明抽成 `releaseNotes.ts`，后续每次发版维护同一份数据即可。

### 版本检查

- 新增 GitHub Release 在线检查，发现新版本时可直接打开 APK 下载入口。
- 支持显示当前版本、最新版本和检查状态。
- 新增版本比较回归测试，覆盖 `test.60`、`test.61` 和普通语义版本比较。

### 发布

- 生成签名 Android release APK：`lifelog-v0.1.0-test.61.apk`。
- Android `versionCode` 更新为 `61`，`versionName` 更新为 `0.1.0-test.61`。

---

## v0.1.0-test.60 (2026-05-21)

### 分享导入确认

- Android 系统分享和手动粘贴分享文本后，先展示识别结果确认卡，不再直接覆盖地点表单。
- 支持“应用到表单”和“只补空白项”，减少误覆盖手动填写内容的风险。
- 确认卡会展示名称、分类、位置、地址、平台和链接，识别为空时保留手动填写流程。

### 分享解析样本库

- 分享解析样本拆分到独立 `place-share-samples.cjs`，便于持续补充真实分享文本。
- Parser 回归地点样本从 12 个扩展到 17 个，新增高德“去这里”、美团店铺卡、点评口味 / 环境 / 服务、抖音团购和小红书种草样本。
- 增强地点字段别名、平台开场文案、坐标文本、分类、店名和分店拆分识别。

### 地点重复处理

- 地点页“重复地点”改为“疑似重复地点”，分开展示强重复和待确认重复。
- 新增强重复 / 待确认数量摘要，强重复可批量合并，弱重复保留逐条确认入口。

### 上下文快速回忆

- 从人物详情、地点详情或回忆详情继续记录时，会自动带入当前人物和地点。
- 快速回忆表单新增“已自动关联”卡片和标题模板按钮，可一键生成“和某人在某地”等标题。
- 新增快速回忆上下文回归测试，覆盖人物、地点、多人和模板生成。

### 发布

- 生成签名 Android release APK：`lifelog-v0.1.0-test.60.apk`。
- Android `versionCode` 更新为 `60`，`versionName` 更新为 `0.1.0-test.60`。

---

## v0.1.0-test.59 (2026-05-21)

### 地点平台链接二期

- 平台店铺链接预设扩展为高德、美团、大众点评、抖音、小红书、百度地图、腾讯地图、微信、官网 / 公众号和自定义。
- 粘贴链接时会自动识别平台并归位到对应预设，例如小红书、百度地图、腾讯地图、微信和抖音短链。
- 平台按钮支持再次点击取消选择，不再需要进入行内手动移除预设平台。
- 地点详情页展示全部已保存平台链接，不再只展示美团和点评。

### 分享解析样本库

- 分享解析新增小红书笔记、抖音短链、百度地图、腾讯地图 scheme 和微信位置样本。
- Parser 回归地点样本从 7 个扩展到 12 个，平台链接解析覆盖小红书和百度地图。
- 优化书店分类、银泰城 / 宝龙城商场名识别和“分享一家店”标题清洗。

### 人物档案修复

- 喜好 / 禁忌项目输入框去掉长 placeholder，避免输入内容被提示文字挤占。
- 下方说明继续保留“、”和“；”批量添加提示。

### 发布

- 生成签名 Android release APK：`lifelog-v0.1.0-test.59.apk`。
- Android `versionCode` 更新为 `59`，`versionName` 更新为 `0.1.0-test.59`。

---

## v0.1.0-test.58 (2026-05-21)

### 地点平台链接

- 地点编辑中的平台店铺链接从多行文本改为预制平台选择，支持高德、美团、大众点评、抖音和自定义链接。
- 点击平台后可单独配置对应店铺链接，未填写链接的平台不会保存。
- 继续兼容原来的“平台 | 链接”保存格式，旧数据无需迁移。
- 快速新增地点时统一平台链接提交来源，避免同名字段导致保存结果不确定。

### 人物档案 UI

- 优化喜好 / 禁忌标签显示，长文本可完整换行展示，删除按钮不再挤压内容。
- 更新喜好 / 禁忌输入提示，明确支持用“、”或“；”一次添加多个项目。

### 发布

- 生成签名 Android release APK：`lifelog-v0.1.0-test.58.apk`。
- Android `versionCode` 更新为 `58`，`versionName` 更新为 `0.1.0-test.58`。

---

## v0.1.0-test.57 (2026-05-20)

### 快速回忆优化

- 快速回忆推断改为同时读取标题和补充正文，避免展开“补充细节”后丢失标题里的日期、人物或地点线索。
- 相对日期以表单选择日期为基准，支持前天、昨天、昨晚、今天、今晚、明天、后天。
- 支持 `2026.5.18`、`5/2`、`5-2`、`5月2日` 等常见日期写法。
- 地点推断优先匹配店名 / 分店名，再匹配商场 / 区域；遇到同一商场多个地点时保持为空，避免误关联。

### 录入与复用

- 快速记录预览与实际保存逻辑保持一致，人物、地点、日期和补充正文变化会实时反映到保存预览。
- 从人物详情、地点详情或回忆详情继续记录时，会自动带入已有上下文；快速记录人物选择支持多人关联。
- 回忆详情新增“再记一条相关回忆”，可基于当前回忆的人物和地点快速补记。

### 显示与筛选修复

- 修复空地点或已删除地点被当成“未关联地点”参与标题生成的问题，避免出现伪标题。
- 回忆详情未关联地点时显示明确空态，不再展示可误触的空地点按钮。
- 修复地点筛选中“未设置”“未分组”无法匹配空字段地点的问题。

### 回归测试

- 新增快速回忆推断回归脚本，覆盖 16 个推断场景。
- 新增回忆显示回归脚本，覆盖空地点 / 已删除地点展示。
- 新增地点位置筛选回归脚本，覆盖未设置省市和未分组区域。
- 保存 helper 回归扩展到 18 个用例，覆盖快速回忆保存、上下文关联、心情清空和照片保留。

### 发布

- 生成签名 Android release APK：`lifelog-v0.1.0-test.57.apk`。
- Android `versionCode` 更新为 `57`，`versionName` 更新为 `0.1.0-test.57`。

---

## v0.1.0-test.56 (2026-05-20)

### 人物档案修复

- 修复编辑人物时清空喜好 / 禁忌后，再次编辑又恢复默认内容的问题。
- 喜好 / 禁忌项目改为标签式编辑，可逐个删除项目。
- 明确提示可用“、”或“；”批量输入喜好项目。

### 录入与解析优化

- 标签输入统一支持顿号、逗号、分号和换行分隔。
- 地点平台链接改为多行编辑，保留美团、点评、抖音和自定义链接。
- 高德 / 美团 / 点评分享解析新增 Android scheme、美团外卖 scheme、点评短链和换行字段覆盖。
- 优化商场名选择，优先保留更完整的商场实体名。

### 数据一致性

- 编辑已有地点时，清空省份、城市或商场会保持为空，不再被默认城市或地址推断回填。
- 编辑已有回忆时，清空心情会保持为空，不再回填默认心情。
- 关于页和备份导出的 appVersion 改为读取同一版本来源。

### 发布

- 生成签名 Android release APK：`lifelog-v0.1.0-test.56.apk`。
- Android `versionCode` 更新为 `56`，`versionName` 更新为 `0.1.0-test.56`。

---

## v0.1.0-test.55 (2026-05-20)

### Android 分享导入

- 新增 Android 系统分享文本入口，可从高德、美团、大众点评等 App 分享地点到 LifeLog。
- 冷启动和重复事件做了去重与延迟投递处理，降低分享文本丢失概率。
- 分享成功后自动打开地点新增面板，并预填识别出的地点草稿。

### 地点分享解析增强

- 高德 / 美团 / 点评分享解析支持多 URL、平台深链、hash/query 参数和嵌套跳转链接。
- 支持同一行字段解析，例如“店名、地址、人均、评分”连在一行的分享文本。
- 增强店名、地址、商场、分店、城市、省份、评分、人均和分类识别。
- 美团 / 点评平台链接会保存到地点资料，地点详情新增“打开点评”入口。

### 备份与提醒

- 账号页新增备份健康检查，展示数据数量、照片引用和潜在风险。
- 导入备份前增加预检，帮助判断备份内容是否完整。
- 提醒设置页新增调度摘要、下次提醒和权限阻断提示。

### 发布

- 生成签名 Android release APK：`lifelog-v0.1.0-test.55.apk`。
- Android `versionCode` 更新为 `55`，`versionName` 更新为 `0.1.0-test.55`。

---

## v0.1.0-test.43 (2026-05-14)

### 🎨 UI/UX 改进

**详情页折叠优化**
- 重构详情页 hero 卡片折叠机制，采用平滑动画过渡
- 折叠阈值优化为 60px，适配内容较少的页面
- 展开阈值设为 0，必须滚动到顶部才展开，避免反复切换
- 动画时长统一为 0.35s，使用 ease-out-quad 缓动曲线
- 添加 400ms 抑制窗口，防止动画期间反复触发
- 调整 `.main-content` 底部留白为 20vh，确保短内容页面也能展开

**日期选择器改进**
- 修复隐藏农历状态时日期数字偏上的问题
- 改用 flex 布局实现垂直居中对齐

**回忆列表对齐**
- 修复回忆卡片中标签和照片数图标不对齐的问题
- 清除 `.memory-tags-line` 内 `.tags` 的 `margin-top`

### 📅 农历显示优化

- 统一干支生肖格式为"丙午马年"（原"丙午年 属马"）
- 更新 `date.ts` 中 `ganZhiText` 和 `ganZhiZodiacText` 生成逻辑
- 移除 PersonDetail 中重复的"属X"显示
- 日历和日期选择器中农历信息显示更简洁

### 🐛 Bug 修复

- 修复商场详情页折叠功能缺失
- 修复地点详情页折叠功能缺失
- 修复回忆详情页折叠后无法展开的问题

### 🔧 技术改进

- 优化 `useCollapsingDetailHeader` hook 性能
- 使用 requestAnimationFrame 节流滚动事件
- 添加调试日志便于问题排查

---

## v0.1.0-test.42 及更早版本

详见 Git 提交历史。
